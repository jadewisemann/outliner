# Outliner

Dynalist을 대신하는 로컬 우선(local-first) 아웃라이너. 브라우저에서 열고, 키보드만으로 쓰고,
데이터는 내 기기에 남고, 내 기기끼리는 알아서 합쳐진다.

```bash
npm install
npm run dev        # http://localhost:5173
npm test           # 코어 로직 단위 테스트
npm run test:e2e   # 실제 브라우저에서의 편집·동기화 시나리오
npm run typecheck
```

## 지금 되는 것

- 무한 깊이 아웃라인, 접기/펼치기, 노드 확대(zoom)와 브레드크럼
- 키보드 우선 편집: Enter 분리, Tab/Shift+Tab, Backspace 병합, ⌘⇧↑↓ 이동 (`⌘/` 로 전체 목록)
- Esc 로 항목 단위 선택 → 여러 줄 한 번에 들여쓰기·이동·삭제·복사
- 메모(Shift+Enter), 체크박스, 제목 수준, 인라인 마크다운, `#태그`, `[[문서 링크]]`
- 문서 여러 개, 사이드바에서 이름 변경·순서 변경·삭제
- 워크스페이스 전체 검색과 태그 검색 (⌘K)
- **기기 간 동기화** — 오프라인 편집도 잃지 않고 병합. 백엔드는 아무 JSON `GET`/`PUT` 서버 또는 **GitHub 저장소** (문서당 파일 하나, 커밋 히스토리 = 버전 백업)
- **GitHub으로 로그인** — 배포에 OAuth function이 있으면 토큰 붙여넣기 대신 버튼 하나. 없으면 PAT 경로가 그대로 동작
- Markdown / OPML / 텍스트 가져오기·내보내기, JSON 전체 백업
- IndexedDB 자동 저장, 실행 취소/다시 실행, 라이트·다크 테마
- 마우스: bullet 드래그로 이동, bullet 클릭으로 확대

## 구조

도메인별 폴더. 각 도메인 안에서 로직은 `.ts`(순수 함수)와 훅으로, 렌더링은 `components/`로
나뉜다. **컴포넌트는 렌더링만 한다** — 예컨대 `Outline.tsx`는 50줄이고 모든 동작은
`useOutline.ts`에 있다.

```
api/            Vercel serverless — OAuth code↔token 교환 (client secret 보관처)
src/
  types.ts      Node / Doc / Workspace 모델
  store.ts      상태 + 자동 저장 + 동기화 루프
  history.ts    실행 취소 — 되돌린 내용이 동기화에서 살아남도록 재스탬프
  app/          레이아웃 껍데기: App, Sidebar, Shortcuts, ErrorBoundary
  outline/      트리 연산(tree.ts), 인라인 마크다운, 가상화, useOutline(모든 동작)
    components/   Outline, Row, Editable — 렌더링만
  sync/         merge.ts(병합 규칙)
    api/          remote.ts(REST·GitHub 백엔드), githubAuth.ts(OAuth 플로)
    components/   SyncSettings, SyncBadge
  storage/      persist(IndexedDB), migrate(스키마), validate(신뢰 경계)
  search/       검색·태그 집계 + SearchPanel
  transfer/     Markdown/OPML/백업 변환 + useTransfer(파일 입출력)
  shared/       order(정렬 키), clock(논리 시계), download, Panel(모달)
  styles/global.css
```

## 설계상의 선택

### 한 줄 = 하나의 `<textarea>`

포커스가 없으면 렌더된 마크다운, 있으면 textarea. contenteditable이나 에디터 프레임워크를
쓰지 않는다. 덕분에 한글 IME 조합이 브라우저 기본 동작 그대로 굴러가고, 조합 중 상태를
되돌리는 코드가 필요 없다. 포커스가 있는 동안 textarea는 **비제어(uncontrolled)** 로 두고,
바깥에서 들어온 값이 마지막으로 내보낸 값과 다를 때만 DOM에 덮어쓴다.

### 동기화 엔진 대신 병합되는 데이터 모델

범용 CRDT 라이브러리를 얹는 대신, 아웃라인이 병합되기 위해 실제로 필요한 세 가지만 모델에
넣었다.

1. **`parent` + `sort`** — 형제 순서를 배열이 아니라 노드마다 가진 분수 인덱스 키로 표현한다
   (`order.ts`). 두 기기가 같은 자리에 각각 줄을 넣어도 둘 다 남는다.
2. **내용과 위치를 따로 스탬프** — 한쪽에서 글을 고치고 다른 쪽에서 옮겨도 둘 다 반영된다.
3. **묘비(tombstone)** — 오래된 기기가 삭제된 줄을 되살리지 못한다. 단, 삭제 *이후에* 그 줄을
   고쳤다면 되살아난다.

병합은 `merge.ts`의 순수 함수 하나이고, 순서와 무관하며 여러 번 적용해도 같은 결과가 된다.
전송 계층의 계약은 "버전 붙은 JSON을 읽고, compare-and-swap으로 쓴다"가 전부라 의존성이 0이다.
이 계약의 구현이 둘 있다: 아무 `GET`/`PUT` 엔드포인트(Firebase RTDB 경로 포함), 그리고
**GitHub contents API** — `GET`이 내용+`sha`를 주고 `PUT`이 그 `sha`를 요구하는 구조가 계약과
정확히 일치해서, 낡은 `sha` 거부(409)가 기존 pull-merge-push 재시도 루프에 그대로 물린다.
푸시마다 커밋이 생기므로 저장소 히스토리가 공짜 버전 아카이브가 된다. 백엔드마다 케이던스가
다르다 — 내 서버는 1.5초/10초, GitHub은 커밋 소음과 rate limit을 고려해 10초/30초에 조건부
GET(304는 한도에 안 잡힘)을 쓴다. 원격이 이미 다 가진 상태에서는 푸시 자체를 건너뛰어, 놀고
있는 기기가 빈 커밋을 남기지 않는다. 같은 브라우저의 다른 탭도 병합 관점에서는 그냥 또 하나의
기기다.

### GitHub에서는 문서 하나가 파일 하나

```
{폴더}/docs/{문서 id}.json   문서 하나 통째로
{폴더}/graves.json           삭제된 문서의 묘비
```

파일 하나로 두면 모든 커밋이 "전부 다시 씀"이라 무엇이 바뀌었는지 히스토리가 말해주지 않는다.
쪼개면 푸시가 실제로 고친 문서만 싣고, 커밋 로그가 편집 로그가 된다. 파일은 **키를 정렬해
들여쓴 JSON**으로 쓴다 — 같은 내용이면 어느 기기가 조립했든 바이트가 같아서 안 고친 문서는
커밋이 아예 안 생기고, 한 줄 편집이 diff에서 한 줄로 보인다.

pull은 폴더 목록 조건부 `GET` 하나로 끝난다. 목록이 주는 git blob `sha`가 곧 내용 해시라
`sha`가 그대로인 문서는 받지도 않는다 — 문서가 몇 개든 유휴 상태의 폴링은 304 두 번이고,
304는 rate limit에 잡히지 않는다.

여러 파일에 걸친 쓰기가 원자적일 필요는 없다. 병합이 순서 무관이라 중간 상태를 읽은 기기는
"어떤 기기가 가질 수 있었던 상태"를 볼 뿐이고 다음 주기에 수렴한다. 순서가 중요한 지점은
**묘비를 먼저 쓰고 파일을 나중에 지운다** 하나뿐이다 — 반대로 하면 중간에 읽은 기기가 없는
파일을 "내가 올려야 할 문서"로 보고 되살린다. 같은 이유로 **파일 삭제는 묘비만이 시킨다**:
페이로드에 없다는 것은 삭제의 증거가 아니라 읽지 못한 파일의 모습이기도 하다.

REST 백엔드는 파일 하나 그대로다. URL 하나가 계약이고, 커밋 히스토리가 없으니 쪼갤 이유도 없다.

`children` 배열은 `parent`/`sort`에서 파생된 캐시다. 읽기가 병합보다 훨씬 잦아서 유지한다.
불일치는 `tree.test.ts`가 모든 연산마다 자동으로 검사한다.

### 트리 연산은 순수 함수, 쓰기는 draft 위에서

`tree.ts`의 함수는 `Doc`을 받아 새 `Doc`을 돌려준다. 다만 내부에서는 노드 맵을 한 번만
얕은 복사한 뒤(*draft*) 그 위에서 바꾼다. 연산마다 전체 맵을 복사하면 1만 줄 붙여넣기가
제곱 시간이 된다 — 실제로 57초였고, draft 방식으로 1초 아래가 됐다.

### 긴 문서는 창(window) 렌더링

행이 250개를 넘으면 화면 근처의 행만 그린다. 그보다 짧으면 전부 그려서 동작이 달라질 여지를
없앤다. 행 높이는 제각각이라, 화면에 나온 적 있는 행은 실제 높이를 기억하고 나머지는
추정값을 쓴다.

측정값 (Chromium, 개발 빌드):

| 노드 수 | 붙여넣기 | DOM에 있는 행 | 키 입력당 동기 작업 |
| --- | --- | --- | --- |
| 2,000 | 0.66초 | 30 | 1.6ms |
| 10,000 | 0.74초 | 30 | 5.6ms |

아무 일도 없는 탭에서 6초간 DOM 변경: 0건. 저장·알림은 타이머 폴링이 아니라 실제 변경에만
반응하고, 원격에서 새로 가져온 게 없는 병합은 같은 객체를 그대로 돌려주므로 React가 렌더를
건너뛴다.

### 신뢰 경계와 실패 처리

바깥에서 오는 데이터(동기화 응답, 가져오기 파일, 저장소에 남아 있던 값)는 전부 `validate.ts`를
통과한다. 원칙은 **던지지 말고 버리기** — 망가진 항목만 버리고 나머지는 살린다. 덕분에 적대적이거나
고장 난 서버가 문서를 지우거나 앱을 죽일 수 없다. 아주 먼 미래의 타임스탬프는 현재로 당겨오는데,
그대로 받아들이면 이 기기의 논리 시계가 포화돼 이후 모든 편집이 병합에서 지기 때문이다.

렌더가 실패해도 막다른 길이 되지 않도록 오류 경계가 백업 내려받기를 제공하고, 로컬 저장이
실패하면 조용히 넘어가지 않고 상단에 배너로 알린다.

## 알려진 한계

- **같은 줄의 같은 필드를 두 기기가 동시에 고치면 한쪽이 이긴다.** 개인이 자기 기기들을
  오가는 용도에서는 맞는 트레이드다. 글자 단위 병합은 기계 장치가 한 자릿수 더 커진다.
- **한 문서를 고치면 그 문서 전체를 올린다.** GitHub에서는 안 고친 문서는 건드리지 않지만
  (REST는 여전히 통짜 하나), 한 문서 안에서는 줄 하나를 고쳐도 문서 전부가 올라간다. 문서 하나가
  수만 노드가 되면 차이(delta) 전송이 필요해진다.
- **원격에서 새 내용을 가져오면 실행 취소 기록이 비워진다.** 그 이전 스냅샷을 되돌리면 이 기기가
  만들지 않은 줄을 지우게 되기 때문이다. 로컬 편집만 있는 동안에는 그대로 쌓인다.
- **30일 넘게 꺼져 있던 기기는 그동안 지워진 줄을 되살릴 수 있다.** 묘비를 영원히 들고 있지
  않기 위한 값이고, 모든 기기가 같은 규칙으로 잊으므로 서로 어긋나지는 않는다.
- 모바일 터치 조작(들여쓰기 버튼, 스와이프)은 아직 없다.
- `Node` 타입 이름이 DOM의 `Node`를 가린다. DOM을 함께 다루는 파일에서 헷갈릴 수 있다.

## 테스트

`src/*/__tests__/`는 깨지기 쉬운 순수 로직만 본다 — 정렬 키, 트리 연산, 병합 규칙,
가져오기/내보내기 왕복, 인라인 파싱. 그중 `merge.test.ts`가 "두 기기가 각자 편집",
"삭제가 되살아나지 않음", "서로를 서로의 자식으로 옮겨 생긴 순환 복구" 같은 실제 사고를 다룬다.
`github.test.ts`는 메모리 안의 가짜 저장소를 상대로 "고친 문서만 커밋된다", "묘비가 파일 삭제보다
먼저 올라간다", "예전 단일 파일에서 넘어온다"를 고정한다.

UI 동작은 실제 브라우저에서 확인한다 — `e2e/outline.spec.ts`(편집·IME·드래그),
`e2e/sync.spec.ts`(가짜 원격 서버를 사이에 둔 두 기기, 적대적인 서버, 죽은 서버, 두 탭),
`e2e/large.spec.ts`(2,000줄에서의 DOM 크기와 응답성).
구현을 그대로 옮겨 적는 테스트는 두지 않는다.

로컬에 Playwright 브라우저가 따로 없다면:

```bash
PLAYWRIGHT_CHROMIUM_PATH=/path/to/chrome npm run test:e2e
```

## 배포

정적 빌드(`dist/`)를 아무 정적 호스팅에 올리면 된다. 서버가 없으므로 도메인은 공개여도
안전하다 — 노트는 각 기기의 IndexedDB와 백엔드에만 있다.

GitHub 로그인을 켜려면 (Vercel 기준): GitHub OAuth App을 만들고 callback URL을 배포 origin으로,
`GITHUB_CLIENT_ID` / `GITHUB_CLIENT_SECRET`를 환경 변수로 넣으면 `api/github-oauth.ts`가
활성화된다. 이 변수가 없으면 로그인 버튼이 숨고 PAT 입력만 남는다. 단, 토큰은 신뢰할 수 있는
배포(본인이 올린 것)에만 입력할 것 — 코드를 서빙하는 쪽이 곧 신뢰 경계다.

## 남은 것

- 레퍼런스 셀프 호스트 서버 (정적 파일 + `GET`/`PUT` + `If-Match`, 파일 하나면 된다)
- 계정과 관리형 동기화 서버 (지금은 주소를 직접 넣어야 한다)
- 날짜 항목과 일정 표기
- 모바일 터치 조작
- 아주 큰 워크스페이스를 위한 delta 동기화
