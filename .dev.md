# Outliner

Dynalist을 대신하는 로컬 우선(local-first) 아웃라이너. 브라우저에서 열고, 키보드만으로 쓰고, 데이터는
내 기기에 남는다.

```bash
npm install
npm run dev        # http://localhost:5173
npm test           # 코어 로직 단위 테스트
npm run test:e2e   # 실제 브라우저에서의 편집 시나리오
npm run typecheck
```

## 지금 되는 것

- 무한 깊이 아웃라인, 접기/펼치기, 노드 확대(zoom)와 브레드크럼
- 키보드 우선 편집: Enter 분리, Tab/Shift+Tab, Backspace 병합, ⌘⇧↑↓ 이동 (`⌘/` 로 전체 목록)
- Esc 로 항목 단위 선택 → 여러 줄 한 번에 들여쓰기·이동·삭제·복사
- 메모(Shift+Enter), 체크박스, 제목 수준, 인라인 마크다운, `#태그`, `[[문서 링크]]`
- 문서 여러 개, 사이드바에서 이름 변경·순서 변경·삭제
- 워크스페이스 전체 검색과 태그 검색 (⌘K)
- Markdown / OPML / 텍스트 가져오기·내보내기, JSON 전체 백업
- IndexedDB 자동 저장, 실행 취소/다시 실행, 라이트·다크 테마
- 마우스: bullet 드래그로 이동, bullet 클릭으로 확대

## 구조

```
src/core/     순수 로직 — React 없음
  types.ts    Node / Doc / Workspace 모델
  tree.ts     모든 트리 연산 (분리·병합·들여쓰기·이동·일괄 처리)
  inline.tsx  인라인 마크다운 렌더링과 캐럿 위치 역매핑
  formats.ts  Markdown / OPML / 텍스트 / 백업 변환
  search.ts   워크스페이스 전체 검색과 태그 집계
  persist.ts  IndexedDB (localStorage 폴백)
  store.ts    상태 + 실행 취소 + 자동 저장 훅
src/ui/       화면 — 상태는 store 하나에서만 온다
  App.tsx       레이아웃, 전역 단축키, 가져오기/내보내기
  Outline.tsx   행 목록, 키보드 처리, 선택, 드래그
  Row.tsx       한 줄
  Editable.tsx  자동 확장 textarea
```

### 설계상의 선택

**한 줄 = 하나의 `<textarea>`, 포커스가 없으면 렌더된 마크다운.** contenteditable이나
에디터 프레임워크를 쓰지 않는다. 덕분에 한글 IME 조합이 브라우저 기본 동작 그대로 굴러가고,
조합 중 상태를 되돌리는 코드가 필요 없다. 포커스가 있는 동안 textarea는 **비제어(uncontrolled)** 로
두고, 바깥에서 들어온 값이 마지막으로 내보낸 값과 다를 때만 DOM에 덮어쓴다.

**트리 연산은 전부 순수 함수.** `tree.ts`의 함수는 `Doc`을 받아 새 `Doc`(과 포커스 요청)을 돌려준다.
구조 공유(copy-on-write)라서 실행 취소 스택은 스냅샷을 그대로 쌓아도 싸다.

**동기화는 아직 없다.** Yjs/Firebase 기반 CRDT 동기화 계층은 이번 재작성에서 걷어냈다.
기기 간 이동은 JSON 백업 내보내기/가져오기로 한다. 다시 넣는다면 `persist.ts` 뒤에 한 겹으로,
`Workspace` 스냅샷 단위 병합부터 시작하는 게 맞다.

**행 가상화(virtualization)는 없다.** `Row`는 `memo`되어 있고 노드는 copy-on-write라 타이핑할 때
실제로 다시 그려지는 행은 하나다. 수만 줄에서 첫 렌더가 느려지면 그때 창(window) 렌더링을 넣는다.

## 테스트

`src/core/*.test.ts`는 깨지기 쉬운 순수 로직만 본다 — 트리 연산, 가져오기/내보내기 왕복,
인라인 파싱. UI 동작은 `e2e/outline.spec.ts`에서 실제 브라우저로 확인한다.
구현을 그대로 옮겨 적는 테스트는 두지 않는다.

로컬에 Playwright 브라우저가 따로 없다면:

```bash
PLAYWRIGHT_CHROMIUM_PATH=/path/to/chrome npm run test:e2e
```

## 남은 것

- 기기 간 동기화 (계정 + 스냅샷 병합)
- 날짜 항목과 `!(날짜)` 같은 일정 표기
- 모바일 터치 조작 (들여쓰기 버튼, 스와이프)
- 아주 큰 문서에서의 행 가상화

과거 요구사항 문서는 [docs/requirements](./docs/requirements/index.md),
결정 기록은 [docs/decisions](./docs/decisions/index.md)에 남아 있다.
