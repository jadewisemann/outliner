# 다이나리스트 격차 분석

> "백본은 잡혔다. 그래서 **다이나리스트를 끊으려면** 무엇이 더 필요한가."
>
> 기능 목록의 비교표가 아니라, 다이나리스트를 매일 쓰는 사람이 **손에 익은 동작**을 기준으로
> 정리한 것이다. 아키텍처와 설계 근거는 [`.dev.md`](./.dev.md)가 정본이고, 이 문서는
> "무엇이 빠졌고, 이 코드베이스에서 그걸 넣으려면 어디를 건드리는가"만 다룬다.

## 한 줄 요약

**구조(아웃라인 편집·동기화·저장)는 이미 다이나리스트와 같은 급이거나 더 낫다.
빠진 것은 전부 "노트를 쓰는 일" 위에 얹힌 층이다** — 날짜, 검색을 필터로 쓰는 방식,
문서가 많아졌을 때의 항해(폴더·즐겨찾기·문서 간 이동), 그리고 오프라인으로 여는 것.

P0 여덟 개를 채우면 "다이나리스트에서 옮겨와서 계속 쓸 수 있다"가 성립한다.
그중 **어느 것도 병합 모델을 바꾸지 않는다** — 이게 이 분석의 가장 중요한 결론이다.

---

## 1. 다이나리스트 경험의 축

다이나리스트가 파는 것은 "들여쓰기 되는 목록"이 아니다. 그건 공짜다. 실제로 사람을 묶어두는
것은 아래 여덟 개이고, 각각이 독립적으로 이탈 사유가 된다.

| # | 축 | 다이나리스트에서의 체감 | 우리 상태 |
| --- | --- | --- | --- |
| 1 | 항목 편집 | 한 줄이 곧 하나의 단위, 키보드만으로 전부 | **동등 이상** |
| 2 | 항해 (많은 문서) | 폴더·즐겨찾기·문서 간 이동이 전부 키보드 한 번 | **거의 없음** |
| 3 | 검색이 곧 뷰 | 검색하면 결과가 *제자리에서* 필터된 트리로 남고 거기서 바로 고친다 | **다름** (결과 목록) |
| 4 | 날짜와 할 일 | `!`로 날짜, 지난 것은 빨강, `within:` 검색이 곧 오늘 할 일 | **없음** |
| 5 | 표시 조절 | 완료 숨기기, 메모 숨기기, 색 라벨, 번호 목록 | **거의 없음** |
| 6 | 항상 열린다 | 데스크톱·모바일 앱, 신호 없어도 열림 | **못 엶** (셸 캐시 없음) |
| 7 | 안전망 | 버전 히스토리, 휴지통, 자동 백업 | **부분** (커밋 히스토리만, 앱에서 못 봄) |
| 8 | 협업 | 링크 공유, 사용자 간 공유, 실시간 | **의도적으로 없음** (§5) |

---

## 2. 축별 상세

### 2.1 항목 편집 — 이미 동등하거나 낫다

되는 것: 무한 깊이, 접기, 확대(zoom)와 브레드크럼, Enter 분리, Tab/Shift+Tab, Backspace 병합,
⌘⇧↑↓ 이동, Esc 항목 선택 후 일괄 조작, 메모, 체크박스, 제목 수준, 인라인 마크다운, `#태그`,
드래그, 붙여넣기로 트리 삽입, 실행 취소.

**빠진 것 세 가지.** 크기는 작지만 매일 눈에 밟히는 종류다.

| 빠진 것 | 다이나리스트 | 여기서 필요한 것 |
| --- | --- | --- |
| 체크리스트 토글 | 체크박스는 **부모를 체크리스트로 지정했을 때만** 그 자식들에 뜬다 | 지금은 `Row.tsx`가 모든 행에 체크박스를 그린다. 부모에 `checklist: boolean`을 두고 자식 렌더에서 참조 |
| 번호 목록 | 부모를 번호 목록으로 바꾸면 자식이 1. 2. 3. | 부모에 `numbered: boolean`. `Row`가 이미 `row.index`를 알고 있어서 렌더만 바뀐다 |
| 색 라벨 | 6색, 항목 메뉴 또는 단축키로 즉시 분류 | 노드에 `color: 0..6`. CSS 변수 하나 |

셋 다 **노드의 필드 하나**이고, `edited` 스탬프의 LWW를 그대로 탄다.
`merge.ts`도 `validate.ts`도 필드 추가 이상의 변경이 없다.

항목 메뉴(bullet 우클릭)가 없다는 것도 여기 붙는다 — 색·번호·체크리스트·링크 복사·문서 간
이동이 모두 갈 곳이 그 메뉴다. 하나를 만들면 나머지가 붙을 자리가 생긴다.

### 2.2 항해 — 가장 크게 빠진 곳

지금 사이드바는 **평평한 문서 목록** 하나다. 문서가 다섯 개면 충분하지만 쉰 개가 되면 무너진다.
다이나리스트 사용자는 보통 수십~수백 개를 폴더에 넣어 쓴다.

| 빠진 것 | 왜 필요한가 | 구현 방향 |
| --- | --- | --- |
| **폴더** | 문서 50개는 평평한 목록으로 못 산다 | `Doc`에 `parent: Id \| null` + 기존 `sort` 분수 인덱스. 폴더도 문서와 같은 테이블의 레코드(`kind: "folder"`)로 두면 **정렬·이동·삭제·병합이 전부 공짜로 따라온다** — 노드 트리에서 이미 검증된 패턴이다. GitHub 레이아웃은 그대로 평평하다(`docs/{id}.json`) — 폴더 소속은 문서의 필드일 뿐이라 파일 경로가 아니다 |
| **즐겨찾기(Bookmarks)** | 자주 쓰는 문서·항목·검색으로 한 번에 | 워크스페이스 레벨 `bookmarks`. 문서/노드/검색어 셋 다 가리킬 수 있어야 하고, 같은 `sort`+묘비 패턴으로 병합된다. **`views`에 넣으면 안 된다** — `views`는 기기 로컬이고 즐겨찾기는 사용자 자산이다 |
| **빠른 전환 / 항목 찾기** | 문서든 항목이든 퍼지 검색으로 점프 | `search.ts` 위에 퍼지 스코어링. 데이터 변경 0 |
| **문서 간 항목 이동** | 인박스에 쌓고 나중에 옮기는 워크플로의 심장 | 아래 주의 |

**문서 간 이동에는 순서 규칙이 하나 필요하다.** `tree.ts`의 연산은 전부 한 `Doc` 안에서 돈다.
문서 A → B 이동은 A에 묘비를 쓰고 B에 삽입하는 두 문서의 편집이다. 노드 id는 UUID라 그대로
들고 가면 되지만, 병합은 문서별로 돌기 때문에 **B만 받고 A를 아직 못 받은 기기는 같은 항목을
두 곳에서 본다.** 수렴은 다음 주기에 되므로 데이터가 깨지지는 않지만, 규칙은
`.dev.md`의 "묘비를 먼저 쓴다"와 같은 계열로 명시해야 한다 — **A의 묘비가 B의 삽입보다 먼저
푸시된다.** 반대면 중간에 읽은 기기가 원본을 "내가 올려야 할 것"으로 되살린다.

### 2.3 검색이 곧 뷰 — 종류가 다르다

이게 기능 하나가 아니라 **모드의 차이**라서 따로 둔다.

지금 `⌘K`는 패널을 띄우고 히트를 **목록**으로 보여준다. 결과를 누르면 그리로 이동한다.
다이나리스트는 반대다 — 문서 안에서 검색하면 화면이 그대로 있고 **매치되지 않는 줄만 사라진다.**
조상은 맥락으로 남고, 남은 줄은 **그 자리에서 편집된다.** 그래서 다이나리스트에서 검색은
"찾기"가 아니라 "임시로 만든 뷰"다. 할 일 관리가 통째로 이 위에 서 있다.

필요한 것:

1. **문서 내 필터 뷰** — `rows` 파생 단계에서 매치 노드 + 그 조상만 남긴다. 데이터 변경 0,
   `useVirtualRows`와 그대로 맞물린다. 편집이 그대로 되는 게 핵심이라 별도 렌더 경로를 만들면 안 된다.
2. **검색 연산자** — 다이나리스트가 실제로 쓰이는 이유의 절반이다.

| 연산자 | 지금 가능한가 | 필요한 것 |
| --- | --- | --- |
| `#태그` | ✅ (계층 태그까지) | — |
| `is:completed` / `is:incomplete` | ❌ | `done` 읽기만 |
| `has:note` / `has:image` / `has:link` | ❌ | 파싱만 |
| `-제외`, `"따옴표 구절"` | ❌ | 쿼리 파서 |
| `within:today`, `within:1w` | ❌ | **날짜 기능 선행** (§2.4) |
| `edited:3d` | ❌ | `edited.at` 읽기만 |
| `created:3d` | ❌ | **`created` 스탬프가 없다** — 아래 |
| `parent:`/`ancestor:` | ❌ | `ancestors()` 이미 있음 |

`created: Stamp` 추가가 유일한 스키마 변경이다. 병합 규칙은 **더 이른 쪽을 취한다**(LWW가 아니라
min) — 생성 시각은 나중에 바뀔 수 없는 값이기 때문이다. `validate.ts`에서 미래 시각을 당겨오는
기존 처리는 그대로 적용된다.

3. **저장된 검색** — 즐겨찾기(§2.2)에 검색어를 담을 수 있으면 자동으로 따라온다.
   "오늘 할 일" 뷰가 여기서 나온다.

### 2.4 날짜와 할 일 — 통째로 없는 기둥

다이나리스트에서 `!`를 치면 날짜 선택기가 뜨고, 고른 날짜는 항목 안에 남아
평소에는 "오늘 오후 3시" 같은 **상대 표기**로 보이며, 지났으면 빨갛게 뜬다.
그리고 `within:today` 검색을 즐겨찾기에 걸어두면 그게 곧 오늘의 목록이 된다.

**중요한 설계 판단: 날짜를 필드로 만들지 말고 `text` 안의 인라인 토큰으로 둔다.**

이유가 셋이다. (1) 스키마 변경이 0이라 병합·검증·GitHub 직렬화가 전부 그대로다.
(2) `inline.tsx`가 이미 `#태그`·`[[문서]]`를 토큰으로 다루고 `sourceOffset`이 렌더↔소스
오프셋을 왕복시키므로, 날짜 토큰은 **패턴 하나 추가**다. (3) Markdown/OPML로 내보내도 날짜가
살아남는다 — 필드로 두면 내보내기에서 증발한다.

필요한 조각:

- `inline.tsx`에 날짜 토큰 패턴 + 상대 표기 렌더 + 지난 날짜 스타일
- `!` 입력 시 날짜 선택기 (달력 위젯 하나. 의존성 없이 가능)
- `within:` / `over:` 검색 연산자 (§2.3의 파서에 붙는다)
- 반복 날짜 — 토큰 안에 반복 규칙을 같이 적고, 완료 시 다음 날짜로 굴린다.
  **완료가 반복을 굴린다**는 규칙이 병합에서 위험하지 않은지는 확인이 필요하다
  (두 기기가 각각 완료하면 두 번 굴러갈 수 있다 — 굴린 결과가 같은 날짜면 LWW로 수렴한다)
- 캘린더 연동: 구글 캘린더 API 대신 **`calendar.ics`를 리포에 커밋**하고 캘린더 앱이 raw URL을
  구독하게 하면 서버 없이 같은 효과가 난다. 단 **E2EE를 켜면 성립하지 않는다** — 구독하는 쪽이
  평문을 읽어야 하기 때문이다. "읽히는 히스토리와 암호화를 함께 가질 수 없다"와 같은 트레이드다

### 2.5 표시 조절

| 빠진 것 | 비용 | 비고 |
| --- | --- | --- |
| 완료 항목 숨기기 | 낮음 | `rows` 파생에서 거른다. **문서별·기기별** 설정이라 `DocView`에 (동기화 대상 아님) |
| 메모 숨기기 | 낮음 | 위와 같은 자리 |
| 글꼴·줄 간격·너비 | 낮음 | CSS 변수 + 설정 패널 |
| 이미지 인라인 | 낮음(원격 URL) / 높음(업로드) | `![](url)` 렌더는 토큰 하나. **업로드**는 GitHub contents API에 바이너리를 커밋하면 서버 없이 되지만, E2EE를 켠 워크스페이스에서는 blob도 봉해야 하고 그러면 `<img src>`로 못 건다 |
| 멀티라인 코드 블록 | 낮음 | 행 텍스트는 한 줄이 원칙(=textarea 하나)이므로 **메모 안의 ``` 펜스**로 두는 게 맞다 |
| LaTeX | **중간, 원칙 충돌** | KaTeX는 첫 런타임 의존성이 된다(번들 71KB → 수백 KB). 정말 필요한 사람만 쓰는 기능이라 지연 로드로 격리하거나, 안 하는 편이 낫다 |
| 커스텀 단축키 | 중간 | 지금 키 처리는 `useOutline.ts`에 하드코딩. 키맵 테이블로 한 겹 빼는 리팩터가 선행 |

### 2.6 항상 열린다 — 오프라인

이건 기능이 아니라 **신뢰**의 문제다. 다이나리스트는 데스크톱 앱과 모바일 앱이 있고 신호가
없어도 열린다. 지금 우리는 매니페스트가 있어 홈 화면에 담기지만, **셸을 캐시하지 않아서
비행기에서 아이콘을 누르면 빈 화면이 뜬다.** 노트는 이미 IndexedDB에 다 있는데도 그렇다.

로컬 우선을 표방하면서 오프라인으로 안 열리는 건 그 자체로 모순이라, 이건 P0다.
service worker 하나 (`HANDOVER.md`의 다음 작업 큐 2번). CSP는 이미 `script-src 'self'`라 충돌 없다.

같이 딸려오는 것: 매니페스트에 **Web Share Target**을 선언하면 폰의 공유 시트에서 바로
인박스로 캡처가 들어온다 — 다이나리스트 모바일 앱의 퀵 캡처에 해당하는 것을 네이티브 앱 없이
얻는다. 데스크톱 앱은 필요해지면 Tauri지만, 브라우저 설치 PWA로 대부분 대체된다.

### 2.7 안전망

| 다이나리스트 | 우리 | 격차 |
| --- | --- | --- |
| 버전 히스토리 (Pro) | **모든 푸시가 커밋.** 문서당 파일이라 diff가 읽힌다 | 데이터는 이미 우리가 낫다. **앱 안에서 볼 수가 없다** — 문서 히스토리 패널(커밋 목록 → 미리보기 → 그 시점으로 복원)이 필요. 커밋 히스토리 API 호출 하나 |
| 자동 백업 (Dropbox/Drive, Pro) | 리포 자체가 백업 + JSON 내보내기 | 없음 |
| 휴지통 (삭제 복구) | 없음 | 아래 |

**휴지통은 묘비와 정면으로 부딪힌다.** 지금 `graves`는 `Record<Id, Stamp>` — id와 시각만 있고
**내용이 없다.** 그래서 "지운 걸 되살리기"는 원리적으로 불가능하다. 내용까지 들고 있으면 30일치
삭제 페이로드가 매 푸시마다 따라다닌다.

권장: **항목 단위 휴지통은 만들지 않는다.** 실행 취소가 짧은 실수를 받고, 긴 실수는 커밋
히스토리가 받는다(§위). 대신 **문서 단위 휴지통**은 만들 값어치가 있다 — 실수로 문서를 지우면
지금은 되돌릴 길이 없고, 문서 수는 적어서 30일 보관이 싸다.

---

## 3. 데이터 이주 — 옮겨오지 못하면 나머지는 의미 없다

기능이 다 있어도 **기존 노트가 그대로 넘어오지 않으면 아무도 옮기지 않는다.**
다이나리스트는 OPML / 서식 있는 텍스트 / 일반 텍스트로 내보낸다.

지금 `formats.ts`의 OPML 가져오기가 읽는 것: `text`, `_note`, `_complete`, `_collapsed`.
다이나리스트 OPML이 실을 수 있는 것 중 **버려지고 있는 것**: 색 라벨, 체크리스트/번호 목록 표시,
제목 수준, 그리고 항목 id(내부 링크의 대상).

해야 할 것:

1. 다이나리스트 내보내기 샘플로 **왕복 테스트 고정** (`formats.test.ts`에 실물 픽스처)
2. §2.1의 필드(색·번호·체크리스트)가 들어오는 대로 OPML 매핑 추가
3. **항목 id 보존** — 안 하면 다이나리스트 안의 내부 링크가 전부 끊긴다.
   id를 보존하면 링크는 §4의 항목 링크로 그대로 이어진다
4. 문서 여러 개를 **한 번에** 가져오기 (지금은 파일 하나씩). 다이나리스트 전체 백업은 문서 뭉치다
5. 폴더 구조 보존 (§2.2가 선행)

이건 P0인데 **§2.1과 §2.2가 먼저 있어야 손실 없이 끝난다.** 순서가 여기서 결정된다.

---

## 4. 항목 단위 내부 링크

지금 `[[...]]`는 **문서 제목**을 가리킨다. 다이나리스트의 `[[`는 **항목**을 가리키고, 이게
"모든 것이 아웃라인 안에 있다"를 성립시키는 접착제다.

노드 id가 이미 전역 유일한 UUID라 조건은 갖춰져 있다. 필요한 것은 (1) 링크 토큰이 id를 싣는 것,
(2) 워크스페이스 전역 id→(문서,노드) 인덱스 — `allTags()`가 이미 같은 모양으로 전 문서를 훑는다,
(3) 대상이 묘비에 묻혔을 때의 표시(깨진 링크를 조용히 지우지 않는다).

**백링크**("이 항목을 가리키는 곳")는 그 인덱스를 뒤집으면 파생으로 나온다. 스키마 변경 0.
다이나리스트에는 기본 기능으로 없는 것이라 여기서 앞설 수 있는 지점이기도 하다.

---

## 5. 하지 않을 것 (그리고 그 이유)

목록에 없어서가 아니라 **의도적으로 뺀 것**이다. 뒤집으려면 대가를 알고 뒤집어야 한다.

| 다이나리스트 기능 | 왜 안 하는가 |
| --- | --- |
| 실시간 공동 편집, 사용자 간 공유, 폴더 공유 | 병합이 클라이언트에 있고 계정이 없다. 서버가 판정하게 만드는 순간 **E2EE가 불가능해지고 GitHub 백엔드도 불가능해진다** (`HANDOVER.md` 결정 2). 개인의 여러 기기가 이 앱의 대상이다 |
| 공개 링크 공유 | 위와 별개로 가능은 하다 — 문서를 정적 HTML로 내보내 Pages에 올리는 방식이면 서버 없이 읽기 전용 공유가 된다. 우선순위는 낮다 |
| 구글 캘린더 양방향 동기화 | OAuth + 서버가 필요. 단방향은 §2.4의 `.ics` 커밋으로 대체 |
| 파일 첨부 | GitHub 백엔드에서는 가능하나 리포가 커지고 E2EE와 충돌한다. 이미지부터 보고 판단 |
| 글자 단위 동시 편집 병합 | `.dev.md`의 알려진 한계 그대로. 개인 기기 왕복에서는 맞는 트레이드 |

---

## 6. 우선순위

### P0 — "다이나리스트를 끊을 수 있다"가 성립하는 최소 집합

순서가 의미 있다. 앞의 것이 뒤의 것의 전제다.

1. **표시 필드 3종** — 체크리스트 토글 / 번호 목록 / 색 라벨 (§2.1). 이주(§3)의 전제
2. **폴더 + 즐겨찾기 + 빠른 전환** (§2.2). 이주의 전제
3. **다이나리스트 데이터 무손실 이주** (§3)
4. **날짜** — 인라인 토큰, 선택기, 상대 표기, 지난 것 강조 (§2.4)
5. **검색 연산자 + 문서 내 필터 뷰** (§2.3). 4를 삼켜서 "오늘 할 일"이 여기서 완성된다
6. **저장된 검색** — 2와 5가 있으면 거의 공짜
7. **완료 항목 숨기기 / 메모 숨기기** (§2.5)
8. **service worker** — 오프라인으로 열리기 (§2.6)

9번이 있다면 **문서 간 항목 이동**(§2.2)이다. 인박스 워크플로를 쓰는 사람에게는 P0,
아니면 P1이다.

### P1 — 옮겨온 다음에 아쉬워지는 것

항목 단위 내부 링크 + 백링크 (§4) · 문서 히스토리 뷰와 복원 (§2.7) · 문서 휴지통 ·
반복 날짜 · 이미지 인라인 표시 · 항목 메뉴(bullet 우클릭) · 표시 설정(글꼴·간격·너비) ·
Web Share Target 캡처

### P2 — 필요해지면

메모 안의 코드 블록 하이라이트 · 커스텀 단축키(키맵 리팩터 선행) · 첨부 파일 ·
`.ics` 내보내기 · 모바일 스와이프 제스처 · LaTeX(의존성 판단 필요) · Tauri 데스크톱

---

## 7. 스키마 변경 총계

P0 전체를 통과시키는 데 필요한 **모델 변경의 전부**다. 이게 이 문서의 결론이다 —
**`merge.ts`의 규칙은 한 줄도 바뀌지 않는다.**

| 대상 | 필드 | 병합 규칙 |
| --- | --- | --- |
| `Node` | `checklist: boolean` | `edited` LWW (기존) |
| `Node` | `numbered: boolean` | `edited` LWW (기존) |
| `Node` | `color: 0..6` | `edited` LWW (기존) |
| `Node` | `created: Stamp` | **더 이른 쪽** (새 규칙 하나) |
| `Doc` | `parent: Id \| null`, `kind: "doc" \| "folder"` | `moved` LWW (기존 `sort`와 짝) |
| `Workspace` | `bookmarks` | `sort` + 묘비 (기존 패턴) |
| `DocView` | `hideCompleted`, `hideNotes`, `filter` | 동기화 안 함 (기기 로컬) |

날짜는 `text` 안의 토큰이라 **여기에 없다** — 그게 인라인으로 두는 이유다.

`migrate.ts`에 version 5가 하나 늘고, `validate.ts`에 필드별 검사가 붙는다.
바깥에서 오는 데이터는 반드시 그 경계를 통과해야 한다는 규칙은 그대로다.

---

## 8. 이미 우리가 나은 지점

옮겨갈 이유이기도 하므로 같이 적어둔다.

- **버전 히스토리가 무제한이고 공짜다.** 다이나리스트에서는 Pro 기능이고, 여기서는 푸시가
  곧 커밋이다. 문서당 파일이라 diff가 "무엇을 고쳤는지"를 그대로 말한다
- **종단 간 암호화.** 다이나리스트에는 없다. 저장소를 가진 쪽이 크기와 시각만 본다
- **데이터가 내 것이다.** 서비스가 닫혀도 리포에 JSON이 남는다. 다이나리스트가 사실상 개발이
  멈춘 지금, 이게 이 프로젝트가 존재하는 이유다
- **오프라인 편집이 손실 없이 병합된다.** 서버 없이, 순서 무관하게
- **의존성 0** (react/react-dom 제외), 번들 71KB gzip
- **한글 IME가 브라우저 기본 동작 그대로** — 에디터 프레임워크를 안 쓴 대가로 얻은 것
