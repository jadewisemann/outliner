# code-rationale — 실측값 · 함정 · 실패한 대안

**값을 바꾸거나 단순화하기 전에 해당 심볼을 여기서 먼저 찾아본다.** 코드 주석을 슬림하게
유지하는 대신 근거를 이곳에 집적한다.

## 실측값

- **draft 패턴** (`outline/tree.ts`): 연산마다 노드 맵 전체를 복사하면 1만 줄 붙여넣기가
  제곱 시간 — 실측 57초. 맵을 한 번만 얕은 복사하는 draft 방식으로 1초 아래.
- **가상화 경계 250행** (`outline/useVirtualRows.ts`): 그 이하는 전부 그려서 동작이 달라질
  여지를 없앤다. 측정값 (Chromium, 개발 빌드):

  | 노드 수 | 붙여넣기 | DOM에 있는 행 | 키 입력당 동기 작업 |
  | --- | --- | --- | --- |
  | 2,000 | 0.66초 | 30 | 1.6ms |
  | 10,000 | 0.74초 | 30 | 5.6ms |

- **유휴 탭 6초간 DOM 변경 0건**이 기준선이다. 저장·알림은 타이머 폴링이 아니라 실제 변경에만
  반응하고, no-op 병합은 같은 객체를 돌려줘 React가 렌더를 건너뛴다.
- **동기화 케이던스**: REST 1.5초/10초, GitHub 10초/30초 (`sync/api/remote.ts`). GitHub 값은
  커밋 소음과 rate limit 때문이고, 조건부 GET의 304는 한도에 잡히지 않는다.
- **묘비 보존 30일**: 더 오래 꺼져 있던 기기는 지워진 줄을 되살릴 수 있다 — 묘비를 영원히
  들고 있지 않기 위한 값 (DESIGN.md 알려진 한계).

## 함정 (전적 있는 것 포함)

- `children`은 `parent`/`sort`의 파생 캐시 — 불일치는 `outline/__tests__/tree.test.ts`의
  `shape()`가 모든 연산에서 자동 검사한다. 트리 연산을 추가하면 이 테스트를 통과해야 한다.
- 바깥에서 오는 데이터는 **반드시** `storage/validate.ts`를 통과시킬 것.
- undo는 스냅샷 복원이 아니라 **재스탬프**다 (`history.ts`) — 아니면 다음 sync가 undo를
  되돌린다.
- 원격 병합 결과는 **객체 동일성**으로 no-op을 판별한다 — 병합/트리 코드에서 불필요한 객체
  재생성은 유휴 리렌더 루프를 되살린다 (전적: 유휴 탭이 분당 50회 리렌더).
- GitHub 백엔드에서 **파일 삭제는 묘비만이 시킨다.** 페이로드에 없다는 건 삭제의 증거가
  아니라 읽지 못한 파일의 모습이기도 하다. 같은 이유로 못 읽는 원격(`locked`)에서는 푸시를
  아예 멈춘다.
- 푸시 여부는 **평문** 직렬화 비교로 정한다 — 암호문은 IV 때문에 매번 다르다.
- 가상화의 "포커스 행으로 스크롤"은 **새 포커스 요청(`seq` 변화)에만** 반응해야 한다. 창
  위치에 반응하게 만들면 편집 중인 행에서 스크롤해 벗어나는 게 불가능해진다 (전적: 1초 뒤
  도로 끌려오던 버그, 커밋 `413455e`).
- `Node` 타입 이름이 DOM의 `Node`를 가린다 — DOM을 함께 다루는 파일에서 헷갈릴 수 있다.
- e2e는 웹 서버를 **둘** 띄운다 (dev 5173, `vite preview` 4173). `csp.spec.ts`와
  `install.spec.ts`는 빌드 결과(4173)를 상대로 돈다 — dev 서버에서는 CSP도 매니페스트도
  발효되지 않는다.
- (환경) Playwright 브라우저가 따로 없으면 `PLAYWRIGHT_CHROMIUM_PATH=/path/to/chrome`.
  GitHub 백엔드 e2e는 케이던스 때문에 `test.setTimeout(120_000)` 수준이 필요하다.
- **`npm run typecheck`(`tsc --noEmit`)가 놓치는 걸 `npm run build`(`tsc -b`)가 잡는다.**
  테스트 파일의 타입 오류가 그렇다 — 커밋 전에 build를 한 번 돌릴 것.
- `view.focusId`는 **캐럿이 실제로 간 곳**이다(`requestFocus`가 기록한다). 팔레트의 행 명령과
  리로드 후 착지 지점이 이걸 읽으므로, 새 포커스 경로를 만들면 반드시 통과시킬 것 (전적 있음).
- 자동 서식 되돌리기는 **그 규칙이 덮어쓸 필드만** 스냅샷해야 한다. 고정 필드로 두면 인용을
  되돌려도 인용이 남는다 (전적 있음).
- `inline.tsx`에서 컴포넌트를 `Math`로 이름 짓지 말 것 — 전역 `Math`를 가린다.
- 문서 완전 삭제의 묘비는 **지금 시각으로** 찍는다 — `deleted` 스탬프(한 달 전일 수 있다)를
  그대로 쓰면 TTL이 이미 지나 바로 잊혀지고, 파일을 가진 기기가 문서를 되살린다.

## 실패한 대안

- **Yjs/Lexical/Firebase 기반 9,200줄 산출물 폐기** (커밋 `736c29c`에서 재작성): 범용
  에디터·CRDT 스택 위에서는 한글 IME와 병합 동작을 통제할 수 없었다. 재작성 후 ~5,200줄,
  의존성은 react/react-dom 뿐.
- **서버에서 병합 판정** — 제안됐으나 폐기: [ADR-0002](../adr/0002-client-side-merge.md).
  서버 병합이면 GitHub 백엔드 불가 + E2EE 불가.
- **contenteditable / 에디터 프레임워크** — 한글 IME 조합 중 상태를 되돌리는 코드가
  필요해진다. 행마다 textarea가 브라우저 기본 동작을 그대로 쓴다.
- **동시 삽입 시 sort 키 충돌** (커밋 `669e03d`에서 수정): 두 기기가 같은 자리에 삽입하면 키가
  충돌해 해당 행이 영구 편집 불가가 됐다. 같은 감사에서 부활 문서가 묘비에 다시 묻히는 문제,
  논리 시계 포화, 악성 페이로드 병합 크래시, undo가 sync에 되돌려지는 문제를 수정 —
  `validate.ts`(신뢰 경계)와 `history.ts`(재스탬프 undo)가 이때 신설됐다.
