# PLANS.md — 진행 중 변경의 계획서

계획마다 **목표 / 원칙 / 단계별 체크리스트 / 완료 기준**을 적는다. 끝난 계획은 지우고 결과를
DESIGN.md(및 하위 문서)에 반영한다.

## Dynalist 전환 잔여 — 이주 충실도와 저장 보장

> **상태: 2·4·5·6단계 완료 (2026-08-21). 1·3단계는 사용자가 Dynalist 내보내기 파일을 줘야
> 시작된다.** 단축키 층은 끝났고 (ADR-0006), 코드로 끝낼 수 있는 것은 이번에 끝났다. 남은 둘은
> 실물이 없으면 추정 위에 또 쌓는 일이라 **의도적으로 멈춘 것**이다. 격차 분석의 갱신은
> [docs/parity.md](./docs/parity.md) §3의 실사 표, 판정 근거는
> [IMPLEMENTATION_NOTES.md](./IMPLEMENTATION_NOTES.md) 2026-08-21 항목.

**목표.** 노트를 이 앱으로 옮기는 사람이 (a) 옮기는 과정에서 조용히 잃는 것이 없고,
(b) 옮긴 뒤 데이터가 브라우저 재량으로 사라지지 않게 한다.

**원칙.**

1. **모르는 형식을 추정으로 채우지 않는다.** 지금 임포트는 Dynalist 내보내기 실물을 한 번도
   만난 적이 없다. 1단계가 먼저인 이유이고, **사용자가 파일을 줘야 시작된다** — 없으면
   2·3단계를 추정 위에 또 쌓게 된다.
2. **조용히 잃는 것과 아예 없는 것을 구분한다.** 링크·태그·폴더는 임포트가 "성공"하고 나서
   잃는 것이라 더 나쁘다 — 사용자가 잃은 줄 모른다. 날짜 계열은 애초에 범위 밖이라
   (§「범위에서 뺀 것」) 잃는 게 아니라 없는 것이고, 이 계획에 넣지 않는다.
3. **저장 보장은 기능이 아니라 원칙 문제다.** 로컬 우선을 표방하면서 브라우저가 노트를
   지울 수 있게 두는 것은 DESIGN.md 원칙 1과 부딪힌다. 이걸 **불변식으로 승격할지**는
   구현할 때 판정한다 (승격하면 DESIGN.md 원칙에, 아니면 알려진 한계에).
4. **가장 싼 것부터.** 2단계는 한 줄에 가깝고 효과가 가장 크다. 진짜 의존성은 1→3 하나뿐이라,
   사용자 파일을 기다리는 동안 2·4·5·6은 먼저 할 수 있다.

**단계별 체크리스트.**

- [ ] **1. 실물 OPML 픽스처** — *사용자가 Dynalist 내보내기 파일을 줘야 한다. 여기서 막혀 있다.*
      `transfer/__tests__/formats.test.ts`의 손으로 쓴 한 줄을 실물로 교체하고 왕복
      테스트로 고정한다. 이때 실제 속성 철자와 **내부 링크·첨부·날짜의 실제 표기**를
      확인해서 3단계의 근거로 쓴다. 개인 노트가 섞이지 않은 최소 문서면 된다.
- [x] **2. `navigator.storage.persist()`** — 완료. `storage/persist.ts`의
      `requestPersistence()`를 store가 **켤 때마다** 부르고(등급은 사용자가 정착하면서 바뀐다),
      등급은 동기화 패널에 늘 적혀 있고 「저장 보장 요청」 버튼이 다시 묻는다(Firefox는 프롬프트라
      버튼이 필요하다). **알릴지의 판정: 알린다 — 단, 실제 손실 조건에서만.** 배너는 거절 +
      원격 사본 없음 + 잃을 것 있음(`hasContent`)이 동시에 참일 때만 뜬다. **불변식 승격 판정:
      "persisted 여야 한다"는 불변식이 **아니고**, "묻고, 거절당하면 말한다"가 불변식이다
      (DESIGN.md 원칙 18) — 판정하는 것이 브라우저이므로 코드가 지킬 수 있는 것은 거기까지다.
- [ ] **3. 내부 링크 재작성** (1단계 뒤) — 임포트할 때 Dynalist의 절대 URL 링크를 `((id))`로
      바꾼다. id는 이미 보존되므로 매핑은 있다. **대상이 이번 배치에 없을 때**의 규칙이
      이 단계의 유일한 판정거리다 — DESIGN.md 원칙 16("조용히 지우지 않는다")과 같은 계열로.
- [x] **4. `@태그`** — 완료. `inline.tsx`의 `TAG_SOURCE` 하나가 `#`·`@`를 다 받고 렌더·검색·
      `allTags`·자동완성이 그것을 공유한다. **경계 판정: 시길 앞 글자.** 이메일 주소에는 언제나
      local part가 붙으므로 `jade@example.com`의 `@`는 시길이 아니다 — 규칙 하나로 끝났고 예외
      목록이 없다. 자동완성은 더 엄격하게(앞이 공백이나 `(`) 보고 시길을 `Trigger`로 들고 간다.
      상세는 [editing.md](./docs/design/editing.md) 「태그의 시길은 둘이고」.
- [x] **5. 폴더 구조 보존** — 완료. `importFiles`가 `webkitRelativePath`를 읽어 경로마다 폴더를
      만들고 `Doc.parent`에 넣는다. **판정: 고른 디렉터리 자체도 폴더가 된다**(가리킨 것의 충실한
      독해이고, 한 임포트가 한 자리에 모인다). 같은 이름의 기존 폴더에 **합치지 않는다** — 이름이
      같은 다른 폴더일 수 있다. 폴더 피커는 별도 숨은 입력이다(`webkitdirectory`가 파일 피커를
      바꿔버린다) + 「폴더 가져오기」 명령·메뉴, 확장자는 `IMPORT_EXTENSIONS`로 직접 걸러낸다.
- [x] **6. 공유 캡처 도착지** — 완료. **판정: 워크스페이스 필드도 기기 로컬도 아니라 `Doc.inbox`
      (문서의 필드)다.** 워크스페이스 최상위 필드는 기기 사이를 건너지 않고(`SyncPayload` =
      `docs` + `graves`), 기기 로컬이면 새 기기에서 다시 도착지가 없다. `titleEdited` LWW를 타므로
      새 병합 규칙도 없다 — `bookmarked`와 같은 수법. 스키마 v6 → v7(필드 추가뿐). 하나로 정하는
      것은 읽는 쪽 `inboxDoc`(표시가 가장 새 것, 동률이면 id 순). 표시가 없으면 첫 캡처가 「인박스」
      문서를 만든다. 고르는 방법은 팔레트 명령, 어느 문서인지는 사이드바의 `↓` 표시.
- [x] **7. Reconcile** — 완료. parity.md §3 실사 표·§6 P0 #5·§7 스키마 총계, DESIGN.md 원칙 15
      확장과 원칙 18 신설·알려진 한계, editing.md 태그 절, trust-boundary.md 실패 처리,
      code-rationale.md 함정 6개.

**완료 기준.** 실물 Dynalist 내보내기를 넣었을 때 텍스트·메모·완료·접힘·색·체크리스트·번호·
제목·id·**내부 링크·태그·폴더**가 전부 살아서 들어오고, 그게 왕복 테스트로 고정되어 있다.
그리고 저장 등급이 persisted이거나, 아니라는 사실이 사용자에게 보인다.

> **남은 것은 「실물 파일」 하나다.** 태그·폴더·저장 보장·캡처 도착지는 끝났다. 내부 링크와
> 왕복 테스트는 실제 표기를 모르는 채로는 추정을 코드로 굳히는 일이 되므로 멈춰 있다.
> **egress 제약이 그대로여서**(`help.dynalist.io` 등이 전부 프록시에서 차단) 문서로 확인할
> 방법도 이 환경에는 없다 — 내보내기 파일이 유일한 1차 자료다.
>
> 검증(2026-08-21): 유닛 216(신규 `types.test.ts` 6·`paths.test.ts` 3, 확장 inline·markdown·
> query·migrate·merge), e2e 71(신규 `@태그` 1, 공유 캡처 스펙은 도착지까지 확인하도록 확장)
> 전부 green. `npm run build`도 green.

**이 계획에 넣지 않은 것** (막는 것이긴 하지만 범위·정책 결정이 먼저다)

- **날짜·일정 계열** — 사용자 결정으로 범위 밖(§「범위에서 뺀 것」). 임포트하면 날짜는
  평문으로 남고 `within:` 검색도 없다. 실제로 안 쓰고 있었다면 이 항목은 그냥 지워도 된다.
- **공유·공동 편집** — 계정이 없고 병합이 클라이언트에 있어 원리적으로 불가 (parity.md §5).
- **GitHub 로그인** — 대기열 1번. 지금 Pages 배포에는 `api/` function이 없어 로그인 버튼이
  숨고 PAT 붙여넣기만 살아 있다. 첫날 마찰이지만 데이터를 잃지는 않는다.
- **delta 동기화** — 대기열 4번. 아주 큰 문서에서만 문제가 된다.

---

## 끝난 계획 기록

(단축키 Dynalist 호환 작업은 2026-08-20 완료: `editor`·`dynalist`
두 프리셋과 "바인딩 없음", 팔레트로만 닿던 표시 기능의 키 부여, 도움말 패널의 키맵 연동.
결정은 [ADR-0006](./docs/adr/0006-keymap-presets.md), 동작 상세는
[editing.md](./docs/design/editing.md) 「단축키는 테이블 하나」.
검증: 유닛 203(keymap 19·tree 35), e2e 70(신규 keymap.spec.ts 6) 전부 green.

모듈 리팩터 R1~R4는 2026-08-20 완료.
실행 기록은 [docs/design/refactor-plan.md](./docs/design/refactor-plan.md)의 「실행 현황」,
결과는 DESIGN.md 구조 트리에 반영. R2 완료 기준 전부 충족: 유닛 191·e2e 64 개수 불변
전부 green, `Outline.tsx`/`Row.tsx` 무변경, `useOutline.ts` 834 → 566줄.)

## 대기열 — 계획으로 승격 전

0. 위 「Dynalist 전환 잔여」가 대기열보다 앞이다 — 그게 지금 사용자를 막고 있는 것이다.
1. **GitHub 로그인 켜기 (선택)** — Pages 배포는 2026-08-20 완료: 소유자가 Settings →
   Pages source를 GitHub Actions로 바꿔 deploy 잡까지 green, 이후 main 병합마다 자동
   배포. 남은 것은 로그인 버튼뿐 — Vercel + OAuth App 생성(callback = 배포 origin) +
   `GITHUB_CLIENT_ID`/`GITHUB_CLIENT_SECRET` (README 「Vercel」). 없어도 PAT 경로는 동작.
2. **디자인 리뷰 잔여** — 본문 여백·측정값 미세조정만 남음 (사용자가 실사용에서 거슬리는
   지점을 짚어주면 그때 정확히). 터치바 아이콘화는 2026-08-20 완료 — 글리프를 인라인
   SVG로. 큰 것(지면과 도형, 거터 정렬, 토큰 분리, 마커 통합)은 끝났다.
3. 레퍼런스 셀프 호스트 서버 (정적 파일 + `GET`/`PUT` + `If-Match`, 파일 하나면 된다)
4. 아주 큰 워크스페이스를 위한 delta 동기화
5. R5(`Doc` 판별 유니온 — **물면 그때**)
6. **단축키 잔여** — 프리셋을 기기 사이에서 옮기는 것(지금은 `localStorage`라 기기마다 다시
   고른다), 그리고 Dynalist에 대응 키가 없어 `editor` 값을 물려받은 액션들(취소선·강조·복제·
   명령 팔레트)을 실사용에서 Dynalist 손이 어디로 찾는지 관찰한 뒤 조정. **물면 그때.**

(R6 CSS 분할은 2026-08-20 완료 — tokens/chrome/outline/panels 네 파일, 규칙 순서 불변.
[refactor-plan.md](./docs/design/refactor-plan.md) 「R6」.)

(백엔드 능력 비대칭의 결정은 2026-08-20 해소 — 선을 긋는 쪽으로,
[ADR-0005](./docs/adr/0005-backend-capability-line.md).)

## 범위에서 뺀 것 (다시 논의하려면 근거부터)

- **날짜·일정 계열** — 사용자 결정. [docs/parity.md](./docs/parity.md) §5.
- **Tauri 데스크톱** — PWA가 설치·오프라인·공유 캡처까지 하는 지금, 남는 것은 작업 표시줄
  아이콘 정도인데 대가가 Rust 툴체인과 플랫폼별 빌드다. [docs/parity.md](./docs/parity.md) §6.
- **WYSIWYG(편집 중 서식 표시)** — contenteditable이 필요해서 한글 IME를 반납하게 된다.
